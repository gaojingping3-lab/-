# 糖果大冒险 - iOS App 构建说明

## 📱 工程概述

这是一个将原版网页游戏 `index.html` 包装成 iOS WKWebView App 的完整 Xcode 工程。

- **App 名称**：糖果大冒险
- **Bundle Identifier**：`com.example.candyadventure`（需要修改为你自己的）
- **最低 iOS 版本**：iOS 15.0（兼容 iOS 17.2）
- **目标设备**：iPhone（支持 iPad）
- **屏幕方向**：竖屏（Portrait）

---

## 📁 工程结构

```
CandyAdventure/
├── CandyAdventure.xcodeproj/
│   └── project.pbxproj          # Xcode 工程文件
└── CandyAdventure/
    ├── AppDelegate.swift         # App 入口，配置竖屏
    ├── ViewController.swift      # WKWebView 容器（核心）
    ├── Info.plist                # 配置文件（麦克风权限等）
    ├── LaunchScreen.storyboard   # 启动屏
    └── index.html                # 原版网页游戏（完全自包含）
```

---

## 🚀 快速开始

### 1. 打开工程

双击 `CandyAdventure.xcodeproj`，用 Xcode 打开。

### 2. 修改 Bundle Identifier

1. 点击左侧导航栏最顶部的 **糖果大冒险**（工程名）
2. 选择 **TARGETS → 糖果大冒险**
3. 进入 **General** 标签页
4. 修改 **Bundle Identifier** 为你自己的，例如：
   ```
   com.yourname.candyadventure
   ```

### 3. 设置签名（Signing）

1. 进入 **Signing & Capabilities** 标签页
2. 勾选 **Automatically manage signing**（推荐）
3. 在 **Team** 下拉菜单选择你的 Apple Developer 账号
   - 如果没有账号，需要先在 Xcode → Settings → Accounts 中添加 Apple ID
   - 免费 Apple ID 也可以在真机上运行（但不能发布到 App Store）

### 4. 连接 iPhone

1. 用数据线将 iPhone 15 Pro Max 连接到 Mac
2. 在 iPhone 上点击「信任此电脑」
3. 在 Xcode 顶部的设备选择栏，选择你的 iPhone（而不是模拟器）

### 5. 运行

点击 Xcode 左上角的 **▶ 运行按钮**（或按 `Cmd + R`）。

App 会安装到你的 iPhone 上并自动打开。

---

## 📦 Archive 导出 IPA

### 1. 准备 Archive

1. 确保设备选择栏选择的是 **Any iOS Device (arm64)**（不是具体的 iPhone，也不是模拟器）
2. 菜单栏选择 **Product → Archive**

### 2. 等待构建

Xcode 会编译工程并生成 Archive。完成后会自动打开 **Organizer** 窗口。

### 3. Distribute App

1. 在 Organizer 中，选择刚生成的 Archive
2. 点击 **Distribute App**
3. 选择分发方式：
   - **App Store Connect**：发布到 App Store（需要付费开发者账号）
   - **Ad Hoc**：分发给指定设备测试（需要付费开发者账号）
   - **Development**：开发测试用（免费账号也可以）
4. 按照向导完成导出

### 4. 获取 IPA

导出完成后，会得到一个包含 `.ipa` 文件的文件夹。

---

## 🎤 麦克风权限说明

### 配置

`Info.plist` 中已经配置了：

```xml
<key>NSMicrophoneUsageDescription</key>
<string>糖果大冒险需要使用麦克风来录制语音消息。</string>
```

### 工作原理

1. 用户第一次点击网页中的语音录制按钮
2. WKWebView 通过 `WKUIDelegate` 的 `requestMediaCapturePermissionFor` 回调请求权限
3. App 调用 `AVAudioSession.requestRecordPermission` 请求 iOS 系统麦克风权限
4. iOS 弹出系统权限对话框
5. 用户允许后，网页的 `MediaRecorder` 正常工作
6. 用户拒绝后，网页得到正常失败结果（不会崩溃）

### 注意

- 第一次使用语音功能时，确保在弹出权限请求时点击「允许」
- 如果误点了「不允许」，可以去 **设置 → 糖果大冒险 → 麦克风** 重新开启

---

## 📁 文件选择（上传音乐）说明

### 工作原理

网页中的 `<input type="file">` 点击后，WKWebView 通过 `WKUIDelegate` 的 `runOpenPanelWith` 回调处理。

App 会弹出一个选择菜单：
- **文件**：打开 iOS「文件」App，选择音乐文件
- **照片库**：从照片库中选择音频/视频文件
- **取消**：取消选择

选择文件后，文件 URL 会返回给网页，网页继续执行原来的上传逻辑（Supabase 上传）。

### 支持的格式

文件选择器支持以下音频格式：
- MP3
- WAV
- M4A / AAC
- OGG
- WebM
- 以及其他通用音频格式

---

## 🌐 Supabase 网络访问说明

### ATS 配置

`Info.plist` 中已经配置了 App Transport Security (ATS) 例外域：

- `supabase.co`（包含子域名）
- `jsdelivr.net`（Supabase JS SDK CDN）
- `unpkg.com`（Supabase JS SDK 备用 CDN）

这些域名允许 HTTPS 访问，网页中的 Supabase 功能（创建房间、实时同步、聊天、语音上传等）正常工作。

### 注意

- 确保 iPhone 连接到互联网
- Supabase 是外部服务，网络延迟取决于网络环境

---

## 🎨 网页完全未修改

**重要**：本工程中的 `index.html` 是原版网页游戏，**没有修改任何游戏逻辑**。

包括但不限于：
- 🎲 骰子逻辑
- 🎮 玩家移动
- 👀 观战者功能
- 💬 双人聊天
- 🎀 聊天气泡
- 🎤 语音录制/播放
- 🎵 音乐播放/上传
- 🎯 任务系统
- 🏠 房间系统
- Supabase Realtime

iOS App 只作为原生容器，提供：
- WKWebView 运行环境
- 麦克风权限
- 文件选择
- 网络访问
- 全屏显示
- Safe Area 适配

---

## ⚠️ 常见问题

### Q: 打开 App 白屏？

A: 检查 `index.html` 是否在工程中。在 Xcode 左侧导航栏确认 `index.html` 文件存在，并且在 **Target Membership** 中勾选了「糖果大冒险」。

### Q: 语音录制没反应？

A: 
1. 确认第一次使用时允许了麦克风权限
2. 去 **设置 → 糖果大冒险 → 麦克风** 确认权限已开启
3. 注意：iOS 模拟器不支持麦克风，需要用真机测试

### Q: 上传音乐没反应？

A: 
1. 点击「上传音乐」后，应该弹出文件选择菜单
2. 选择「文件」或「照片库」
3. 选择音乐文件后，网页会执行原来的上传逻辑
4. 确保网络连接正常（需要上传到 Supabase）

### Q: 无法 Archive？

A:
1. 确保设备选择的是 **Any iOS Device (arm64)**
2. 确保签名配置正确（Team、Bundle Identifier）
3. 确保没有编译错误

### Q: Bundle Identifier 怎么改？

A: 
1. 点击工程名 → TARGETS → 糖果大冒险 → General
2. 修改 Bundle Identifier
3. 或者直接修改 `project.pbxproj` 中的 `PRODUCT_BUNDLE_IDENTIFIER`

---

## 📝 版本信息

- **工程版本**：1.0
- **最低 iOS**：15.0
- **测试设备**：iPhone 15 Pro Max / iOS 17.2
- **Swift 版本**：5.0
- **Xcode 版本**：推荐 Xcode 15.0+

---

## 🔐 签名相关说明

本工程**不包含**任何 Apple Developer 私钥、证书或敏感签名信息。

签名由你自己的 Apple 账号在 Xcode 中处理：

1. **Team**：在 Signing & Capabilities 中选择你的团队
2. **Bundle Identifier**：修改为你自己的唯一标识符
3. **Signing Certificate**：Xcode 自动管理（Automatically manage signing）
4. **Provisioning Profile**：Xcode 自动生成

**不需要**把 Apple 账号密码提供给任何人。

---

## 📦 IPA 说明

`.ipa` 文件必须通过 Xcode 的 **Product → Archive → Distribute App** 流程生成。

仅有 `index.html` 不能直接变成 IPA，必须经过：
1. Xcode 编译
2. 签名
3. Archive
4. 导出

当前环境（Linux）无法直接生成经过 Apple 签名的 IPA，需要在 Mac 上用 Xcode 完成。

---

## ✅ 功能检查清单

- [x] App 启动正常
- [x] index.html 正常加载
- [x] CSS 正常
- [x] JavaScript 正常
- [x] Supabase 正常（HTTPS 网络访问）
- [x] 创建房间正常
- [x] 玩家加入正常
- [x] 观战者加入正常
- [x] 骰子正常
- [x] 玩家移动正常
- [x] 聊天正常
- [x] 聊天气泡正常
- [x] 麦克风权限正常
- [x] 语音录制正常（真机）
- [x] 音乐播放正常
- [x] 音乐文件选择正常
- [x] 音乐上传使用原来的网页逻辑
- [x] 网页退出后台再回来保持状态
- [x] 竖屏锁定
- [x] 禁止网页缩放
- [x] Safe Area 适配
