# 糖果大冒险 - 从 ZIP 到 IPA 完整操作指南

> **适用人群**：只有 iPhone，没有 Mac 电脑的用户
>
> **最终目标**：拿到真正的 `CandyAdventure.ipa` 文件，安装到 iPhone

---

## 📋 目录

1. [你现在拥有什么](#一你现在拥有什么)
2. [三种获取 IPA 的方式](#二三种获取-ipa-的方式)
3. [方式一：GitHub Actions 云端构建（推荐）](#方式一github-actions-云端构建推荐)
4. [方式二：找人代建](#方式二找人代建最简单)
5. [方式三：借/租 Mac 一键构建](#方式三借租-mac-一键构建)
6. [如何把 IPA 安装到 iPhone](#如何把-ipa-安装到-iphone)
7. [常见问题](#常见问题)

---

## 一、你现在拥有什么

解压 `糖果大冒险_iOS工程_完整版.zip` 后，你会得到：

```
CandyAdventure/
├── CandyAdventure.xcodeproj/          ← Xcode 工程（双击打开）
├── CandyAdventure/                     ← 源代码
│   ├── AppDelegate.swift
│   ├── ViewController.swift            ← WKWebView 容器
│   ├── Info.plist                      ← 麦克风权限等配置
│   ├── LaunchScreen.storyboard
│   └── index.html                      ← 你的网页游戏（完全未修改）
├── .github/workflows/
│   └── build-ios.yml                   ← GitHub Actions 自动构建配置
├── build_ipa.sh                        ← Mac 一键构建脚本
├── ExportOptions.plist                 ← IPA 导出配置
├── 操作说明.md                          ← 本文档
└── 构建说明.md                          ← 技术说明
```

**关键点**：`.github/workflows/build-ios.yml` 已经配置好了，上传到 GitHub 后，macOS 云服务器会自动帮你构建 IPA。

---

## 二、三种获取 IPA 的方式

| 方式 | 难度 | 费用 | 推荐度 |
|------|------|------|--------|
| GitHub Actions 云端构建 | ⭐⭐ | 免费 | ⭐⭐⭐⭐⭐ |
| 找人代建（淘宝/闲鱼/朋友） | ⭐ | 5-20元 | ⭐⭐⭐⭐ |
| 借/租 Mac 一键构建 | ⭐⭐⭐ | 免费/少量 | ⭐⭐⭐ |

**推荐方式一**：GitHub Actions 完全免费，配置一次以后随时可以构建，不需要电脑。

---

## 三、方式一：GitHub Actions 云端构建（推荐）

### 整体流程

```
iPhone 上操作
↓
注册 GitHub 账号
↓
创建仓库
↓
上传工程文件
↓
配置签名 Secrets
↓
点击 Run workflow
↓
macOS 云服务器自动构建（5-10分钟）
↓
下载 CandyAdventure.ipa
```

---

### 第 1 步：注册 GitHub 账号

1. 用 iPhone Safari 打开 https://github.com
2. 点击右上角 **Sign up**
3. 输入邮箱、密码、用户名
4. 验证邮箱（去邮箱收验证邮件）
5. 登录成功

> 💡 GitHub 是全球最大的代码托管平台，免费注册，免费使用。

---

### 第 2 步：创建新仓库

1. 登录 GitHub 后，点击右上角 **+** 号
2. 选择 **New repository**
3. 填写：
   - **Repository name**：`candy-adventure`（随便起）
   - **Description**：可选，填「糖果大冒险 iOS App」
   - **Public**：选择公开（免费额度更多，macOS 构建免费）
   - 勾选 **Add a README file**
4. 点击底部 **Create repository**

---

### 第 3 步：上传工程文件

1. 在仓库页面，点击 **Add file**（绿色按钮旁边）
2. 选择 **Upload files**
3. 点击 **choose your files**
4. 在 iPhone 文件 App 中找到解压后的 `CandyAdventure` 文件夹
5. **全选所有文件和文件夹**（包括 `.github` 文件夹、`CandyAdventure.xcodeproj`、`CandyAdventure` 文件夹、`build_ipa.sh` 等）
6. 等待上传完成
7. 滚动到页面底部，点击 **Commit changes**

> ⚠️ **重要**：必须上传 `.github` 文件夹，这是自动构建的配置。
>
> 如果 iPhone 上看不到 `.github` 文件夹（以点开头的文件默认隐藏），请：
> - 在文件 App 中，长按空白处，选择「显示隐藏项目」
> - 或者先把所有文件压缩成 zip，上传后在 GitHub 上解压

---

### 第 4 步：验证 GitHub Actions 配置

1. 上传完成后，点击仓库顶部的 **Actions** 标签
2. 你应该能看到左侧有 **Build CandyAdventure IPA** 这个工作流
3. 如果看到了，说明配置上传成功

---

### 第 5 步：配置签名 Secrets（关键步骤）

> ⚠️ **没有签名证书，无法生成可安装的 IPA。**
>
> 这一步需要你有 Apple Developer 账号（免费的 Apple ID 也可以）。

#### 5.1 进入 Secrets 配置页面

1. 在仓库页面，点击 **Settings**（设置）
2. 左侧菜单找到 **Secrets and variables**
3. 点击 **Actions**
4. 点击 **New repository secret**

#### 5.2 需要配置的 5 个 Secrets

| Secret 名称 | 内容 | 如何获取 |
|-------------|------|----------|
| `APPLE_TEAM_ID` | 10位字母数字，如 `ABC1234567` | 见下方 5.3 |
| `IOS_DISTRIBUTION_CERTIFICATE` | 证书的 Base64 编码 | 见下方 5.4 |
| `IOS_DISTRIBUTION_CERTIFICATE_PASSWORD` | 导出证书时设置的密码 | 导出证书时自己设置 |
| `IOS_PROVISIONING_PROFILE` | 描述文件的 Base64 编码 | 见下方 5.5 |
| `KEYCHAIN_PASSWORD` | 随便设一个复杂密码 | 自己随便设，如 `MyKeychain2024!` |

#### 5.3 如何获取 Team ID

1. 用 iPhone Safari 打开 https://developer.apple.com/account
2. 登录你的 Apple ID
3. 点击左侧 **Membership**
4. **Team ID** 就是你需要的（10位字母数字组合）

#### 5.4 如何获取证书（需要 Mac 或找人帮忙）

> ⚠️ 这一步需要 Mac 电脑。如果你没有 Mac，可以：
> - 找朋友帮忙导出证书
> - 淘宝/闲鱼找人代签（5-20元）
> - 或者直接用「方式二：找人代建」

**如果你有 Mac：**

1. 打开 Mac 上的 **Xcode**
2. Xcode → Settings → Accounts → 登录 Apple ID
3. 打开 **钥匙串访问**（Keychain Access）
4. 左侧选择 **登录** → **我的证书**
5. 找到 `Apple Development: 你的邮箱` 或 `iPhone Distribution: 你的名字`
6. 右键 → **导出**
7. 保存为 `certificate.p12`
8. 设置密码（记住这个密码，就是 `IOS_DISTRIBUTION_CERTIFICATE_PASSWORD`）
9. 打开终端，运行：
   ```bash
   base64 -i certificate.p12 -o certificate_base64.txt
   ```
10. 打开 `certificate_base64.txt`，复制全部内容
11. 粘贴到 GitHub Secret `IOS_DISTRIBUTION_CERTIFICATE` 中

#### 5.5 如何获取 Provisioning Profile

1. 登录 https://developer.apple.com/account
2. 进入 **Certificates, Identifiers & Profiles**
3. 左侧点击 **Profiles**
4. 点击 **+** 创建新的描述文件
5. 选择 **iOS App Development**（免费账号）或 **App Store**（付费账号）
6. 选择你的 App ID（如果没有，先去 Identifiers 创建）
7. 选择你的证书
8. 选择你的设备（需要先注册 iPhone 的 UDID）
9. 命名后点击 **Generate**
10. 点击 **Download** 下载 `.mobileprovision` 文件
11. 在 Mac 终端运行：
    ```bash
    base64 -i profile.mobileprovision -o profile_base64.txt
    ```
12. 复制内容到 GitHub Secret `IOS_PROVISIONING_PROFILE`

---

### 第 6 步：触发构建

1. 在仓库页面，点击 **Actions** 标签
2. 左侧选择 **Build CandyAdventure IPA**
3. 点击右侧 **Run workflow**（运行工作流）
4. 选择：
   - **Branch**：`main` 或 `master`
   - **IPA 分发方式**：`development`（免费账号选这个）
   - **Bundle Identifier**：默认即可，或填你自己的
5. 点击 **Run workflow**
6. 刷新页面，你会看到一个新的构建任务正在运行

---

### 第 7 步：等待构建完成

1. 构建通常需要 **5-10 分钟**
2. 点击构建任务可以查看实时日志
3. 如果构建失败，查看错误信息，参考「常见问题」部分
4. 构建成功后，任务前面会显示 ✅

---

### 第 8 步：下载 IPA

1. 点击进入成功的构建任务
2. 滚动到页面最底部的 **Artifacts**（产物）区域
3. 你会看到 **CandyAdventure-IPA**
4. 点击下载
5. 下载后是一个 zip 文件，解压后得到 `CandyAdventure.ipa`

> 🎉 **恭喜！你已经拿到了真正的 IPA 文件！**

---

## 四、方式二：找人代建（最简单）

如果你觉得配置 GitHub Actions 太麻烦，可以直接找人代建。

### 步骤

1. 把 `糖果大冒险_iOS工程_完整版.zip` 发给帮你构建的人
2. 提供你的 **Team ID**（从 https://developer.apple.com/account 获取）
3. 提供你的 **iPhone UDID**（可以通过「爱思助手」或「Apple Configurator」查看）
4. 等待对方返回 `.ipa` 文件
5. 用「爱思助手」或「Sideloadly」安装到 iPhone

### 费用参考

- 淘宝/闲鱼代签 IPA：5-20 元
- 朋友帮忙：免费
- 某些平台提供免费签名服务（但有广告或限制）

---

## 五、方式三：借/租 Mac 一键构建

如果你能借到或租到一台 Mac，可以用一键脚本快速构建。

### 步骤

1. 把 `糖果大冒险_iOS工程_完整版.zip` 传到 Mac
2. 解压
3. 打开 **终端**（Terminal）
4. 进入工程目录：
   ```bash
   cd /path/to/CandyAdventure
   ```
5. 确保 Xcode 已安装并登录 Apple ID
6. 运行一键构建脚本：
   ```bash
   ./build_ipa.sh -t YOUR_TEAM_ID -m development
   ```
7. 等待 3-5 分钟
8. IPA 在 `output/糖果大冒险.ipa`

### 脚本参数

```bash
./build_ipa.sh \
  -t ABC1234567 \          # Team ID（必填）
  -b com.myapp.candy \     # Bundle ID（可选）
  -m development \          # 分发方式（development/ad-hoc/app-store）
  -o ~/Desktop/ipa         # 输出目录（可选）
```

---

## 六、如何把 IPA 安装到 iPhone

拿到 `CandyAdventure.ipa` 后，需要用工具安装到 iPhone。

### 方法一：Sideloadly（推荐，Windows/Mac 都支持）

1. 电脑下载 Sideloadly：https://sideloadly.io
2. iPhone 连接电脑
3. 打开 Sideloadly
4. 把 `CandyAdventure.ipa` 拖进去
5. 输入你的 Apple ID
6. 点击 **Start**
7. 等待安装完成
8. iPhone 上：**设置 → 通用 → VPN与设备管理 → 信任开发者**

### 方法二：爱思助手（Windows，简单）

1. 电脑下载爱思助手
2. iPhone 连接电脑
3. 爱思助手 → **我的设备 → 应用游戏**
4. 点击 **安装本地应用**
5. 选择 `CandyAdventure.ipa`
6. 等待安装完成

### 方法三：AltStore（免费，需要 Wi-Fi）

1. 电脑下载 AltServer：https://altstore.io
2. 安装 AltServer 到电脑
3. iPhone 安装 AltStore（通过 AltServer）
4. 在 AltStore 中添加 IPA 文件
5. 等待安装

### 方法四：Xcode（Mac）

1. Mac 打开 Xcode
2. iPhone 连接 Mac
3. Xcode → **Window → Devices and Simulators**
4. 选择你的 iPhone
5. 点击 **+** 号
6. 选择 `CandyAdventure.ipa`
7. 等待安装

---

## 七、常见问题

### Q1: GitHub Actions 构建失败，提示「签名配置不完整」

**原因**：没有配置签名 Secrets，或者配置不完整。

**解决**：
1. 确认 5 个 Secrets 都已配置：
   - `APPLE_TEAM_ID`
   - `IOS_DISTRIBUTION_CERTIFICATE`
   - `IOS_DISTRIBUTION_CERTIFICATE_PASSWORD`
   - `IOS_PROVISIONING_PROFILE`
   - `KEYCHAIN_PASSWORD`
2. 确认证书没有过期
3. 确认 Provisioning Profile 包含你的设备 UDID

---

### Q2: 构建成功，但 Artifacts 里没有 IPA

**原因**：签名配置不完整，只完成了编译验证，没有生成 IPA。

**解决**：
1. 查看构建日志，确认「导出 IPA」步骤是否执行
2. 如果签名配置不完整，只会生成 Archive，不会生成 IPA
3. 配置完整的签名 Secrets 后重新运行

---

### Q3: 证书怎么获取？我没有 Mac

**解决方法**：
1. **找朋友帮忙**：有 Mac 的朋友可以帮你导出证书
2. **淘宝/闲鱼代签**：搜索「iOS 签名」，5-20 元搞定
3. **使用免费签名服务**：某些平台提供免费签名，但有广告或限制
4. **直接找人代建**：把工程发给对方，让对方直接返回 IPA

---

### Q4: 免费 Apple ID 可以用吗？

**可以**，但有以下限制：
1. 证书有效期 **7 天**，7 天后需要重新签名安装
2. 同一个 Apple ID 最多同时安装 **3 个**自签 App
3. 只能使用 **development** 分发方式
4. 需要注册设备 UDID

**建议**：如果长期使用，可以考虑付费开发者账号（99美元/年），证书有效期 1 年。

---

### Q5: IPA 安装后闪退

**可能原因**：
1. 证书过期（免费账号 7 天过期）
2. 设备未信任开发者
3. 设备 UDID 未包含在 Provisioning Profile 中
4. IPA 损坏

**解决**：
1. 重新签名安装
2. 设置 → 通用 → VPN与设备管理 → 信任开发者
3. 重新生成包含设备 UDID 的 Provisioning Profile
4. 重新下载 IPA

---

### Q6: 语音功能不能用

**解决**：
1. 第一次使用语音时，App 会请求麦克风权限，点击「允许」
2. 如果误点了「不允许」，去 **设置 → 糖果大冒险 → 麦克风** 开启
3. 注意：iOS 模拟器不支持麦克风，需要用真机测试

---

### Q7: 上传音乐没反应

**解决**：
1. 点击「上传音乐」后，应该弹出文件选择菜单
2. 选择「文件」或「照片库」
3. 选择音乐文件后，网页会执行原来的上传逻辑
4. 确保网络连接正常（需要上传到 Supabase）

---

### Q8: GitHub Actions 构建很慢

**正常**：macOS Runner 构建 iOS App 通常需要 5-10 分钟。

如果超过 15 分钟还没完成，可能是：
1. 网络问题
2. 证书安装失败
3. 工程配置问题

查看构建日志，找到卡住的步骤。

---

### Q9: 可以不用签名直接安装吗？

**不可以**。iOS 系统要求所有 App 必须经过 Apple 签名才能安装。

即使是未签名的 IPA，也需要通过侧载工具（如 Sideloadly）用你的 Apple ID 重新签名后才能安装。

---

### Q10: 游戏功能会改变吗？

**不会**。iOS App 只是一个 WKWebView 容器，网页游戏的所有功能（骰子、聊天、语音、音乐、任务、房间等）完全保持原样。

打包后的 App 和你在浏览器中打开的网页游戏体验完全一致。

---

## 📞 需要帮助？

如果在构建过程中遇到问题，请准备以下信息：

1. 构建方式（GitHub Actions / 找人代建 / Mac 脚本）
2. 错误截图或错误日志
3. Apple 账号类型（免费/付费）
4. iOS 版本
5. 构建日志的最后 50 行

把这些信息发给帮你构建的人，或者在 GitHub Issues 中提问。

---

**祝你构建顺利！🎮**

记住：最终目标是拿到 `CandyAdventure.ipa`，然后安装到你的 iPhone 15 Pro Max 上！
